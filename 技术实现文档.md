# 《看山沃野》技术实现文档

> 版本：Demo 1.2（腾讯云 CloudBase 托管部署方案）  
> 技术目标：团队发布一个线上 Demo；评委打开链接、用自己的知乎账号授权，即可读取自己的收藏夹并在不同设备同步农场进度。  
> 部署原则：使用 CloudBase 的 HTTP 云函数与文档型数据库；不购买、不维护云主机。

## 1. 技术结论

《看山沃野》采用 **Next.js 前端＋Node 服务端＋CloudBase 文档型数据库**。

不再采用 SQLite。SQLite 是单个文件，适合本地开发或固定的一台服务器；而 CloudBase 的云函数属于弹性运行环境，不应依赖本地文件持续保存。为实现同一知乎账号在手机、电脑和不同浏览器间同步农场，应使用 CloudBase 内置文档型数据库。

```text
用户浏览器
   │
   ▼
CloudBase HTTP 云函数（Next.js SSR + API Routes）
   ├─ 知乎 OAuth 回调、会话、收藏夹读取
   └─ CloudBase 文档型数据库
          ├─ 农场地块与作物
          ├─ 本轮已重温内容
          └─ 成就与用户设置
   │
   ▼
知乎 OAuth / 知乎数据开放平台
```

CloudBase 为项目提供计算、数据库、HTTPS 域名和弹性运行环境。团队仍拥有“服务端”，但无需管理 Linux、Nginx、Docker、磁盘或数据库进程。

## 2. 为什么选择 CloudBase

| 需求 | CloudBase 对应能力 |
| --- | --- |
| 线上可运行 Demo | HTTP 云函数托管 Next.js，支持 SSR 与 API 路由 |
| 知乎 OAuth | 服务端保存 App Key、处理 callback，避免密钥暴露给浏览器 |
| 跨设备同步 | 文档型数据库保存农场状态 |
| 不想自己买服务器 | 云函数自动运行与扩缩，不需要运维机器 |
| 项目封面/静态资源 | 静态托管或云存储（Demo 初期可直接打包静态资源） |

CloudBase 官方提供 Next.js 在 HTTP 云函数中部署的路径，并建议 Node.js 20；同时提供服务端 Node SDK 与文档型数据库。[Next.js 部署文档](https://docs.cloudbase.net/cloud-function/frameworks-examples/next)；[Node.js SDK 文档](https://docs.cloudbase.net/quick-start/frameworks/nodejs)

## 3. 评委体验路径

```text
评委打开团队提交的 HTTPS Demo 链接
  → 点击“使用知乎登录”
  → 在知乎完成授权
  → 返回《看山沃野》
  → 读取评委自己的收藏夹
  → 选择内容来源、播种、收获
  → 后续在手机或电脑再次登录同一账号，读取同一套农场
```

评委不需要下载仓库、安装 Zhihu CLI、填写 Access Secret 或部署项目。团队只需提交 CloudBase 上发布的 HTTPS 链接。

## 4. 系统架构与数据流

### 4.1 组件职责

| 层 | 职责 | 不能做什么 |
| --- | --- | --- |
| 浏览器 | 农场 UI、动画、用户操作 | 不保存 `APP_KEY`、Access Secret、OAuth token |
| Next.js Route Handlers | OAuth、会话校验、收藏夹接口、农场 API | 不向客户端返回密钥 |
| ZhihuGateway | 调用黑客松版 CLI 或等价官方 OpenAPI | 不将 token 写入日志 |
| CloudBase 文档型数据库 | 保存账号、农场、作物、重温记录、成就 | 不保存收藏正文 |

### 4.2 用户登录与同步流程

```text
1. 用户点击知乎登录
2. 服务端生成 state，跳转知乎授权页
3. 知乎回调服务端，服务端用 code 换 access token
4. 服务端创建加密会话 Cookie，并获得 zhihuUserId
5. 服务端读取数据库：该用户是否已有农场
6. 有农场 → 返回原地块、作物、已重温记录；无农场 → 创建 10 块空地
7. 浏览器调用收藏夹接口，展示当前最新收藏夹数据
```

同一账号换设备时，OAuth 登录后获得同一个 `zhihuUserId`；查询同一份数据库记录，因此农场状态同步。

### 4.3 数据最小化

- 收藏夹和收藏内容默认实时从知乎读取，不将完整正文复制进数据库。
- 数据库仅保存体验必需字段：内容标识、类型、标题、原帖 URL、来源收藏夹、作物时间与重温状态。
- 用户可以在设置中“清除农场数据”；此操作只删除本项目中的状态，不影响知乎原有收藏。

## 5. 数据库设计

CloudBase 使用文档型数据库，不需要自己建 MySQL 表或部署 SQLite 文件。建议创建以下集合（collection）：

| 集合 | 一条记录代表 | 关键字段 |
| --- | --- | --- |
| `users` | 一个已登录知乎用户 | `zhihuUserId`、`displayName`、`createdAt`、`updatedAt` |
| `farms` | 用户的一座农场 | `userId`、`plotCount: 10`、`selectedFavlistTokens`、`currentRoundId` |
| `crops` | 一块地上的一次生长 | `userId`、`plotIndex`、`contentKey`、`stage`、`plantedAt`、`sproutAt`、`maturesAt` |
| `reviewed_items` | 一篇在本轮已收获的内容 | `userId`、`roundId`、`contentKey`、`harvestedAt` |
| `achievements` | 一项已解锁成就 | `userId`、`key`、`unlockedAt` |

### 5.1 关键索引

- `users.zhihuUserId`：唯一索引。
- `farms.userId`：唯一索引。
- `crops`：`(userId, plotIndex)` 唯一，确保一块地同一时间只有一个作物。
- `crops`：`(userId, contentKey)` 唯一，确保同一篇收藏不会同时种在两块地。
- `reviewed_items`：`(userId, roundId, contentKey)` 唯一，确保同一轮不会重复收获。

### 5.2 数据访问规则

客户端不应直接拥有数据库写权限。所有数据库读写通过 Next.js 服务端 API 完成，服务端先用会话确认 `zhihuUserId`，再使用它作为查询条件。

这样用户 A 即使修改浏览器请求，也不能读取或修改用户 B 的农场数据。

## 6. 农场核心规则

### 6.1 状态

每篇收藏内容在一轮重温中处于以下之一：

```text
eligible（待重温）
→ growing（某块地生长中）
→ reviewed（已收获并打开原帖）
```

提前铲除会删除 `crop` 记录，使对应内容恢复为 `eligible`；用户没有真正重温它，因此未来仍可再次随机出现。

### 6.2 播种

1. 用户选择一个空地。
2. 服务端读取本轮已选收藏夹的最新内容。
3. 过滤 `reviewed_items` 与当前 `crops` 中的内容。
4. 从剩余候选中随机选择一篇，以事务/唯一索引写入 `crops`。
5. 返回种子状态；成熟前前端不展示完整标题。

### 6.3 成熟与收获

- `maturesAt` 是绝对时间；请求农场数据时，服务端比较当前时间并返回 `seed`、`sprout` 或 `mature`。
- 不需要 cron。用户再次打开应用时，作物会自动显示正确阶段。
- 用户点击成熟果实即收获：服务端写入 `reviewed_items`、删除 `crop`、释放地块，并返回原帖 URL。
- 不要求用户读完原帖，也不因不操作而惩罚。

## 7. 服务端 API

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `GET` | `/api/auth/zhihu` | 发起 OAuth，生成 state |
| `GET` | `/api/auth/zhihu/callback` | 校验 state，换取 token，创建会话 |
| `POST` | `/api/auth/logout` | 清除会话 |
| `GET` | `/api/me` | 当前用户与基础进度 |
| `GET` | `/api/favlists` | 读取知乎收藏夹列表 |
| `GET` | `/api/favlists/:token/contents` | 读取指定收藏夹内容 |
| `GET` | `/api/farm` | 返回 10 块地、作物和进度 |
| `POST` | `/api/farm/select-sources` | 保存本轮选择的收藏夹 |
| `POST` | `/api/plots/:index/plant` | 随机播种 |
| `POST` | `/api/crops/:id/care` | 可选轻度照料 |
| `DELETE` | `/api/crops/:id` | 铲除作物、释放地块 |
| `POST` | `/api/crops/:id/harvest` | 收获并记录重温 |

## 8. 本地开发准备

### 8.1 安装软件

在自己的电脑上安装：

1. [Node.js 20 LTS](https://nodejs.org/)
2. Git
3. VS Code（推荐，但非必需）
4. 黑客松要求的 Zhihu CLI / skill

打开 PowerShell，进入项目目录后检查：

```powershell
node --version
npm --version
```

Node 显示 20.x 或更高即可。

### 8.2 初始化项目

```powershell
git clone <你们的仓库地址>
cd <项目目录>
npm install
copy .env.example .env.local
npm run dev
```

浏览器打开 `http://localhost:3000`。本地开发阶段可先使用 Mock 收藏数据完成 UI；真实 OAuth 要等 CloudBase 域名和回调地址配置完毕后再联调。

### 8.3 本地环境变量

`.env.local` 只存在你自己的电脑，不能提交到 Git：

```dotenv
# CloudBase
CLOUDBASE_ENV_ID=
CLOUDBASE_SECRET_ID=
CLOUDBASE_SECRET_KEY=

# 知乎
ZHIHU_ACCESS_SECRET=
ZHIHU_OAUTH_APP_ID=
ZHIHU_OAUTH_APP_KEY=
ZHIHU_OAUTH_REDIRECT_URI=

# 应用
SESSION_ENCRYPTION_KEY=
APP_URL=http://localhost:3000
```

`SESSION_ENCRYPTION_KEY` 使用随机、至少 32 个字符的字符串；不使用示例密钥。

## 9. CloudBase 部署：手把手操作

### 第 1 步：准备腾讯云账号

1. 打开 [CloudBase 控制台](https://tcb.cloud.tencent.com/)。
2. 登录腾讯云账号；按控制台提示完成实名认证和云开发开通。
3. 控制台首页会显示“环境 ID”。复制到安全位置，稍后填入 `CLOUDBASE_ENV_ID`。

> 不要把环境 ID、SecretId、SecretKey、知乎 App Key 粘贴到 GitHub、飞书公开文档或演示视频中。

### 第 2 步：创建 CloudBase 环境

1. 在控制台选择“创建环境”或“开通云开发”。
2. 环境名称填写例如 `kanshan-woye-prod`。
3. 地域选择靠近主要评委/用户的区域；保持同一环境下部署、数据库和云函数。
4. 计费方式按控制台给出的黑客松/个人开发提示选择；创建前核对费用说明。
5. 创建完成后记录环境 ID。

### 第 3 步：创建数据库集合

依次进入“数据库”页面，创建：

```text
users
farms
crops
reviewed_items
achievements
```

初期不必手工填充数据，应用首次登录时自动创建对应用户和农场记录。

将集合设置为“仅服务端访问”或等价的私有权限；不要为了省事把所有集合设为公开读写。

### 第 4 步：为项目接入 CloudBase SDK

在项目根目录执行：

```powershell
npm install @cloudbase/node-sdk dotenv
```

服务端创建一个 CloudBase 客户端模块，例如 `src/server/cloudbase.ts`。它只在 Node 端被导入，并从环境变量读取 CloudBase 配置。不要在带有 `"use client"` 的组件中导入它。

### 第 5 步：修改 Next.js 部署配置

CloudBase 的 Next.js HTTP 云函数部署需要独立产物。编辑 `next.config.ts` 或 `next.config.mjs`：

```ts
const nextConfig = {
  output: "standalone",
  images: { unoptimized: true },
  compress: true,
  poweredByHeader: false,
};

export default nextConfig;
```

然后执行：

```powershell
npm run build
```

构建失败先在本地修复，确认成功再上传 CloudBase。

### 第 6 步：部署 Next.js HTTP 云函数

对第一次部署，推荐使用控制台：

1. CloudBase 控制台进入“云函数”。
2. 点击“新建函数”，函数类型选择“HTTP 云函数”。
3. 函数名称可填 `kanshan-woye-web`。
4. 提交方式选择“本地上传文件夹”，选择项目根目录。
5. 运行环境选择 Node.js 20；开启自动安装依赖。
6. 内存先选 512 MB 或更高，再按实际运行情况调整。
7. 点击创建，等待构建和部署结束。

CloudBase 官方也支持 CLI 部署。熟悉命令行后可使用：

```powershell
npm install -g @cloudbase/cli
tcb login
tcb app deploy --framework next --output-dir .next -e <环境ID>
```

第一次建议优先控制台，遇到构建问题时更容易查看日志。

### 第 7 步：配置服务端环境变量

在 HTTP 云函数/应用的“环境变量”设置中，逐项添加第 8.3 节列出的变量。

尤其确认：

- `ZHIHU_OAUTH_APP_KEY` 没有以 `NEXT_PUBLIC_` 开头；
- `CLOUDBASE_SECRET_KEY`、`ZHIHU_ACCESS_SECRET`、OAuth token 都不会被写入前端；
- 环境变量保存后需要重新发布/重启应用才生效。

### 第 8 步：获得 HTTPS 地址并配置 OAuth

1. 在 HTTP 云函数的访问配置中获取默认 HTTPS 地址，或绑定自己的自定义域名。
2. 拼出回调地址：

```text
https://<你的 CloudBase 域名>/api/auth/zhihu/callback
```

3. 到黑客松项目提报/应用创建页，将它填写为“知乎登录回调地址”。
4. 将同一个地址填入 CloudBase 环境变量 `ZHIHU_OAUTH_REDIRECT_URI`，并把 `APP_URL` 改为线上 HTTPS 地址。
5. 重新部署后再测试。

OAuth 回调地址必须完全一致；域名、协议、路径和末尾斜杠任一处不同都可能导致授权失败。

### 第 9 步：首次线上验收

用未参与开发的知乎账号测试：

1. 打开线上链接，确认首页正常显示。
2. 点击“使用知乎登录”，完成授权后能回到农场。
3. 看见该测试账号自己的收藏夹。
4. 选择收藏夹、播种一颗作物、刷新页面。
5. 再次登录同一账号，确认作物仍在同一块地。
6. 换一台设备登录同一账号，确认农场仍同步。
7. 收获一颗成熟果实，确认原帖可打开且该内容本轮不会再次抽到。

## 10. 发布与更新

每次更新代码的安全流程：

```text
本地修改
→ npm run build
→ 本地测试核心流程
→ CloudBase 发布预览版本
→ OAuth、收藏夹、作物状态回归测试
→ 发布正式版本
```

提交黑客松时，将正式 HTTPS 地址填入“可运行体验链接”；项目介绍中明确说明使用了知乎 OAuth、收藏夹/收藏内容能力与 CloudBase 托管部署。

## 11. 常见故障排查

| 现象 | 优先检查 |
| --- | --- |
| OAuth 提示回调不匹配 | 知乎后台登记地址、`ZHIHU_OAUTH_REDIRECT_URI`、实际线上 URL 是否逐字符相同 |
| 登录后收藏夹为空 | OAuth token 是否有效、CLI/API 权限、测试账号是否有收藏夹 |
| 本地能跑、云端构建失败 | Node 版本、`npm run build` 是否成功、依赖是否写入 `package.json` |
| 云端提示找不到数据库 | `CLOUDBASE_ENV_ID`、服务端 SDK 初始化、数据库集合名称是否一致 |
| 刷新后农场丢失 | 服务器是否真的写入 `farms/crops`；不要再只依赖 `localStorage` |
| 换设备不同步 | 登录后用户 ID 是否一致；数据库查询是否按 `zhihuUserId` 读写 |
| 密钥泄露风险 | 立刻在对应平台轮换密钥；检查 Git 历史、日志和部署变量 |

## 12. 安全与隐私

- `.env.local`、任何 Access Secret、App Key、OAuth token 必须在 `.gitignore` 中。
- CloudBase 密钥与知乎密钥只配在服务端环境变量中，不提交源代码。
- 数据库只允许服务端 API 访问；所有查询都按当前 `zhihuUserId` 过滤。
- 不存储收藏正文、Cookie 或明文 OAuth token。
- 提供“清除农场数据”功能，删除该用户在 `farms/crops/reviewed_items/achievements` 中的数据；不删除其知乎收藏。

## 13. 后续扩展

CloudBase 数据库为后续能力留出空间：跨设备同步、历史回顾、地块解锁、成就榜单、推送通知与团队运营后台。它们均不属于当前 Demo 的必做范围，当前优先完成真实登录、真实收藏夹读取、稳定播种与收获闭环。
