# 《看山沃野》产品界面改版规划

> 版本：1.0  
> 状态：已确认方向，待进入线框和实现阶段

## 1. 改版背景

当前项目已经具备农场玩法的基本闭环：选择收藏夹、随机播种、经历种子/幼苗/成熟阶段、浇水、铲除和收获。但当前页面采用“介绍首屏 → 农场区域 → 回顾进度 → 成就 → 页脚”的长页面结构，用户需要通过滚动在不同信息区之间移动，整体更像网站，而不是打开即可操作的小游戏。

本规划将保留现有的玩法规则和低压力原则，重点重构页面容器、信息层级、场景表现和素材接入方式。

## 2. 已确认的设计决策

| 决策项 | 方案 |
| --- | --- |
| 主要设备 | 桌面浏览器优先，同时兼容手机 |
| 产品气质 | 安静、治愈的轻农场，游戏感优先于网站信息展示 |
| 主场景 | 农场占据完整游戏视口，十块地默认一屏可见 |
| 土地扩展 | 主农场保持一屏；新增土地通过农场区域切换进入 |
| 外层滚动 | 禁止；只有必要的浮窗内部允许滚动 |
| 播种方式 | 从已选收藏夹中随机抽取内容 |
| 刘看山 | 主要通过动画参与播种、浇水、成熟和收获反馈 |
| 收获流程 | 成熟作物 → 收获预览浮窗 → 打开知乎原帖 |
| 内容展示 | 浮窗展示标题、来源、类型、标签或摘要线索，不复制完整正文 |
| 回顾功能 | 统一命名为“收获记录”，以浮窗或抽屉呈现 |
| 素材 | 团队制作；先支持 PNG，后续兼容 GIF；通过素材清单接口接入 |
| 互动强度 | 用户不操作也能成熟，浇水等互动只提供轻微加速和反馈 |

## 3. 改版目标

- 用户进入农场后，不需要先阅读大段说明就能理解下一步动作；
- 农场是页面的视觉和交互中心，而不是页面中的一组卡片；
- 种植、等待、收获形成明确而轻量的游戏循环；
- 内容来源、进度、成就等辅助信息不会切碎主场景；
- 桌面端在 1440×900 和 1366×768 等常见尺寸下不出现外层滚动；
- 后续更换素材或增加土地区域时，不需要重写核心玩法。

## 4. 目标信息架构

改版后采用单一游戏壳，不再使用页面锚点导航：

```text
GameShell（100dvh，全屏且禁止外层滚动）
├── GameHud
│   ├── 看山沃野标识
│   ├── 当前内容来源
│   ├── 本轮回顾进度
│   └── 农场地图 / 设置
├── FarmScene
│   ├── 场景背景与装饰
│   ├── 刘看山
│   ├── 十块可见地块
│   └── 未来可解锁区域入口
├── ActionDock
│   ├── 收获记录
│   ├── 内容来源
│   └── 帮助
└── OverlayStack
    ├── 地块详情浮窗
    ├── 来源选择面板
    ├── 收获预览面板
    └── 帮助 / 设置面板
```

首页改为简短的游戏标题画面。进入农场后，介绍性长文、原则说明和页脚不再占用主场景空间。

## 5. 主场景规划

### 5.1 全屏游戏壳

- 使用 `100dvh` 作为主视口高度；
- `html`、`body`、游戏主容器禁止外层滚动；
- 顶部是轻量 HUD，中间是农场场景，底部是操作栏；
- 浮窗、抽屉和详情面板在独立的覆盖层中打开；
- 浮窗内容过长时仅允许浮窗内部滚动。

### 5.2 农场场景

- 桌面端默认展示十块地，并让土地成为一整片有道路、边界、背景和装饰的场景；
- 地块不再使用内容卡片样式；作物阶段主要通过外观、颜色、动效和成熟提示表达；
- 空地只保留一个明确的播种动作；
- 标题、来源、标签、成熟时间等详细信息移入地块详情浮窗；
- 成熟作物应比普通作物具有更强的视觉层级，如呼吸、闪光或轻微摇摆；
- 装饰不得遮挡地块的点击区域。

移动端可以调整为 3×4 的区域排布，但应尽量让十块地仍处于一个游戏视口内。触控目标最小尺寸为 44×44px。

### 5.3 HUD 和操作栏

HUD 只显示用户经常需要知道的状态：当前来源、本轮进度、当前区域和设置入口。进度可以使用 `3/12` 这类简短数字或小型进度环，不再占用一张独立卡片。

底部操作栏提供“收获记录”“内容来源”“帮助”等低频入口。演示加速属于开发/演示能力，不放在正式主导航中。

## 6. 土地扩展规划

十块地是主农场的默认容量。未来增加土地时，不通过无限缩小地块或增加网页滚动实现，而是增加农场区域：

```text
主农场：当前十块地
东侧新田：解锁后切换进入
山坡果园：未来区域
```

区域切换可以通过农场边缘道路、木牌、地图按钮或小型农场地图完成。每个区域拥有独立背景、装饰和地块数量，但复用相同的播种、成长、照料和收获规则。区域和素材接口见《农场素材规范与接口设计》及技术实现文档中的 `FarmZone` 规划。

## 7. 刘看山和动效

刘看山默认以 `idle` 动画待机；关键事件触发一次性动作，结束后回到待机：

| 事件 | 动画 |
| --- | --- |
| 播种成功 | `planting` |
| 浇水成功 | `watering` |
| 发现作物成熟 | `noticing` |
| 收获成功 | `celebrate` |
| 无操作 | `idle` |

推荐的反馈链路：

```text
用户操作 → 地块反馈 → 刘看山动作 → 轻量粒子/提示 → 回到待机
```

动画是情绪反馈，不是任务要求。不能因为用户没有照料而停止作物成长，也不应频繁弹出提示打断操作。继续支持 `prefers-reduced-motion`，减少动态效果时使用静态回退图或 CSS 效果。

## 8. 播种、详情和收获流程

### 8.1 播种

点击空地后直接随机播种，不进入复杂表单。播种成功后，刘看山播放 `planting`，地块显示种子状态，来源收藏夹保留在详情浮窗中。

### 8.2 地块详情

点击地块打开详情浮窗：

- 种子：显示来源、预计成熟时间、等待/浇水/铲除操作；
- 幼苗：显示关键词、标签、标题片段等线索；
- 成熟：显示完整标题、内容类型、来源和收获按钮。

### 8.3 收获预览

点击成熟作物后先完成收获动画，再打开预览浮窗。浮窗展示标题、内容类型、来源收藏夹、标签或摘要线索，并提供“在知乎打开原帖”和“先留在农场”两个动作。

当前版本不在应用内复制完整知乎正文。完整正文回到知乎完成，以保持内容隐私、降低同步复杂度，并避免项目退化为阅读器。

### 8.4 收获记录

“收获记录”是轻量的已重温记录，展示标题、来源、收获时间和重新打开原帖入口。它不是排行榜、任务列表或正文阅读页，优先以浮窗或抽屉形式存在。

## 9. 组件拆分建议

```text
GameShell
├── GameHud
├── FarmScene
│   ├── SceneBackground
│   ├── FarmDecorations
│   ├── Mascot
│   ├── Field
│   │   └── Plot
│   │       ├── PlotGround
│   │       └── CropVisual
│   └── EffectLayer
├── ActionDock
└── OverlayStack
    ├── CropDetailPanel
    ├── SourcePanel
    ├── HarvestPreviewPanel
    └── HarvestLogPanel
```

第一轮实现优先复用当前 `FarmState`、`Crop`、`stageOf`、播种/浇水/铲除/收获函数、本地存储和演示加速。主要改动集中在页面结构、CSS 全屏布局、地块视觉和素材清单接入。

## 10. 验收标准

- 常见桌面分辨率打开农场后不出现页面滚动条；
- 十块地、刘看山和主要 HUD 一屏可见；
- 用户无需滚动即可完成播种、查看地块、浇水和收获；
- 来源、进度和收获记录均可在一到两次点击内打开；
- 成熟作物的视觉层级明显高于普通作物；
- 素材替换无需修改农场状态和核心玩法逻辑；
- 缺少动画素材时仍可使用静态回退图或 CSS 效果完成流程；
- 手机端不产生外层滚动，浮窗内部才允许必要的内容滚动；
- 刷新页面后，已有作物和收获状态仍然保留。

## 11. 实现顺序

1. 建立 `GameShell` 全屏容器和 HUD/操作栏骨架；
2. 将现有农场区域迁移为 `FarmScene`，去掉页面锚点和长页面卡片；
3. 将 `.plot` 重构为场景地块对象，保留现有点击和状态逻辑；
4. 接入素材清单、PNG/GIF 统一加载和回退策略；
5. 增加刘看山事件动画和播种/浇水/收获反馈；
6. 将进度、来源和收获记录迁移到 HUD/浮窗；
7. 按验收标准检查桌面、移动端、键盘焦点和减少动态效果场景。
