# 《看山沃野》农场素材规范与接口设计

> 版本：1.0  
> 适用范围：全屏农场游戏界面、地块、作物、刘看山动画、操作反馈和未来区域扩展

## 1. 设计目标

素材由团队独立制作，当前暂定使用 PNG，后续可能加入 GIF 动画。代码必须通过“语义化素材接口”引用素材，而不是在业务组件中写死图片路径。

需要满足以下目标：

- 替换图片时不修改播种、成长、收获等核心逻辑；
- PNG 和 GIF 可以使用同一套组件加载；
- GIF 缺失或用户减少动态效果时，有静态 PNG 回退；
- 新增作物主题、刘看山动作和农场区域时，不破坏已有存档；
- 素材团队可以按照清单独立交付和验收。

## 2. 推荐目录

素材放在 `public/assets/farm/` 下，按用途分类，而不是按页面分类：

```text
public/
└── assets/
    └── farm/
        ├── scenes/
        │   ├── farm-main-day.png
        │   ├── farm-main-evening.png
        │   └── farm-new-field.png
        │
        ├── terrain/
        │   ├── plot-empty.png
        │   ├── plot-locked.png
        │   ├── plot-soil.png
        │   ├── path.png
        │   └── water-edge.png
        │
        ├── crops/
        │   └── default/
        │       ├── seed.png
        │       ├── sprout.png
        │       └── mature.png
        │
        ├── mascot/
        │   ├── idle.gif
        │   ├── planting.gif
        │   ├── watering.gif
        │   ├── noticing.gif
        │   └── celebrate.gif
        │
        ├── effects/
        │   ├── water.png
        │   ├── sparkle.png
        │   ├── harvest.gif
        │   ├── dust.png
        │   └── land-unlock.gif
        │
        ├── props/
        │   ├── tree-01.png
        │   ├── fence-01.png
        │   ├── signboard.png
        │   ├── stone-01.png
        │   └── flower-01.png
        │
        └── ui/
            ├── panel-paper.png
            ├── button-wood.png
            └── badge-frame.png
```

文件名统一使用英文小写和短横线，例如 `mascot-watering.gif`、`crop-memory-mature.png`。不要使用中文、空格或带日期的文件名。

## 3. 素材清单

### 3.1 农场场景

第一版建议至少准备主农场背景、天空和远山、地面与道路、边缘装饰，以及一处未来区域入口或锁定土地。背景可以先制作成一张完整 PNG，也可以拆成以下多层：

```text
天空层 → 远山层 → 地面层 → 道路层 → 前景装饰层
```

拆层后可以增加季节、昼夜和轻微视差效果，而不影响地块逻辑。

### 3.2 地块

至少需要空地、已开垦土地、锁定土地，以及可选的选中状态和可收获提示。悬停、选中、焦点和禁用状态优先使用 CSS，不必为每种交互状态制作图片。

### 3.3 作物

第一版至少需要三个阶段：

```text
seed.png
sprout.png
mature.png
```

第一版可以只有 `default` 一套通用作物。接口预留 `reading`、`idea`、`city` 等主题扩展，未来可以根据收藏内容类型或标签选择不同植物，但不建议第一版制作大量变体。

### 3.4 刘看山

建议至少准备以下动作：

| 文件 | 用途 | 播放方式 |
| --- | --- | --- |
| `idle` | 无操作待机 | 循环 |
| `planting` | 播种成功 | 播放一次 |
| `watering` | 浇水成功 | 播放一次 |
| `noticing` | 发现成熟作物 | 播放一次 |
| `celebrate` | 收获成功 | 播放一次 |
| `thinking` | 长时间无操作，可选 | 循环或播放一次 |

当前已有的 `public/assets/liukanshan-idle.gif` 可以作为待机素材。新增动画尽量使用相同画布尺寸。

### 3.5 操作反馈

建议准备浇水水滴、播种尘土、成熟闪光、收获粒子、土地解锁和刘看山提示气泡等少量透明效果。静态 PNG 可以配合 CSS 淡入、位移和缩放；需要多帧或连续动作时再使用 GIF。

### 3.6 装饰和 UI

装饰素材包括树、围栏、石头、花、木牌等，不能遮挡地块点击区域。面板、按钮和徽章边框可以先使用 CSS，只有需要明显手工质感时再提供 PNG。按钮文字、标题和状态文字不应烘焙进图片。

## 4. 尺寸和制作规范

### 4.1 场景背景

- 设计基准：`1920×1080`；
- 重要内容不要贴近左右和上下边缘；
- 透明分层时，各层保持相同画布尺寸；
- 使用 sRGB 色彩；
- PNG 应进行压缩，避免一张背景过大影响首次加载。

### 4.2 地块

- 推荐画布：`256×256`；
- 使用透明背景；
- 所有地块底部基线统一；
- 地块主体不要贴到画布边缘；
- 同一组地块使用相同的透视和光照方向。

### 4.3 作物

- 推荐画布：`256×256` 或 `320×320`；
- 使用透明背景；
- 三个成长阶段底部位置必须一致；
- 不在图片中绘制标题、按钮和标签；
- 植物主体不要超出安全区域。

### 4.4 刘看山动画

- 推荐统一画布：`320×320`；
- 角色脚部或站立基线统一；
- 动作动画结束后应能自然回到待机；
- 一次性 GIF 需要提供动画时长；
- 每个 GIF 最好提供对应静态 PNG 回退图。

### 4.5 交互安全区

- 不要把关键角色、果实和按钮画到边缘；
- 点击区域由网页代码控制，不依赖图片透明区域；
- 移动端触控目标至少 `44×44px`；
- 装饰可以被裁切，交互对象不能被裁切。

## 5. TypeScript 素材接口

建议新增文件：

```text
lib/farm-assets.ts
```

基础素材类型：

```ts
export type AssetRef = {
  src: string;
  format: "png" | "gif";
  width: number;
  height: number;
  alt?: string;
  fallback?: string;
  durationMs?: number;
  loop?: boolean;
};
```

完整素材清单类型：

```ts
export type FarmAssetManifest = {
  scenes: {
    main: AssetRef;
    evening?: AssetRef;
    lockedField?: AssetRef;
  };

  terrain: {
    emptyPlot: AssetRef;
    lockedPlot: AssetRef;
    soil: AssetRef;
  };

  crops: Record<
    string,
    {
      seed: AssetRef;
      sprout: AssetRef;
      mature: AssetRef;
    }
  >;

  mascot: {
    idle: AssetRef;
    planting?: AssetRef;
    watering?: AssetRef;
    noticing?: AssetRef;
    celebrate?: AssetRef;
    thinking?: AssetRef;
  };

  effects: {
    water?: AssetRef;
    sparkle?: AssetRef;
    harvest?: AssetRef;
    unlock?: AssetRef;
  };
};
```

示例：

```ts
export const farmAssets: FarmAssetManifest = {
  scenes: {
    main: {
      src: "/assets/farm/scenes/farm-main-day.png",
      format: "png",
      width: 1920,
      height: 1080,
      alt: "看山沃野主农场",
    },
  },

  terrain: {
    emptyPlot: {
      src: "/assets/farm/terrain/plot-empty.png",
      format: "png",
      width: 256,
      height: 256,
      alt: "空地",
    },
    lockedPlot: {
      src: "/assets/farm/terrain/plot-locked.png",
      format: "png",
      width: 256,
      height: 256,
      alt: "尚未解锁的土地",
    },
    soil: {
      src: "/assets/farm/terrain/plot-soil.png",
      format: "png",
      width: 256,
      height: 256,
      alt: "已开垦土地",
    },
  },

  crops: {
    default: {
      seed: {
        src: "/assets/farm/crops/default/seed.png",
        format: "png",
        width: 256,
        height: 256,
      },
      sprout: {
        src: "/assets/farm/crops/default/sprout.png",
        format: "png",
        width: 256,
        height: 256,
      },
      mature: {
        src: "/assets/farm/crops/default/mature.png",
        format: "png",
        width: 256,
        height: 256,
      },
    },
  },

  mascot: {
    idle: {
      src: "/assets/farm/mascot/idle.gif",
      format: "gif",
      width: 320,
      height: 320,
      loop: true,
      fallback: "/assets/farm/mascot/idle.png",
    },
    celebrate: {
      src: "/assets/farm/mascot/celebrate.gif",
      format: "gif",
      width: 320,
      height: 320,
      loop: false,
      durationMs: 1800,
      fallback: "/assets/farm/mascot/idle.png",
    },
  },

  effects: {},
};
```

## 6. 组件使用约定

业务组件只使用语义化查询，不直接拼接文件路径：

```ts
const family = crop.assetFamily ?? "default";
const cropAsset = farmAssets.crops[family][stage];
```

建议组件职责如下：

```text
FarmScene
├── SceneBackground
├── FarmDecorations
├── Mascot
├── Field
│   └── Plot
│       ├── PlotGround
│       └── CropVisual
└── EffectLayer
```

- `SceneBackground`：背景和场景分层；
- `FarmDecorations`：树、围栏、花等非交互装饰；
- `Plot`：地块位置、可点击区域和状态；
- `CropVisual`：根据成长阶段显示作物；
- `Mascot`：管理刘看山动画状态；
- `EffectLayer`：水滴、闪光、收获和解锁效果。

如果某个动画素材暂时不存在，组件按以下顺序回退：

```text
对应动画 GIF
→ fallback 静态 PNG
→ CSS 简单效果
→ 当前默认占位图
```

## 7. 刘看山动画事件接口

建议用语义事件驱动动画，而不是在地块组件里直接操作图片：

```ts
export type MascotAction =
  | "idle"
  | "planting"
  | "watering"
  | "noticing"
  | "celebrate"
  | "thinking";

export type FarmEvent =
  | { type: "PLANT"; plot: number }
  | { type: "CARE"; plot: number }
  | { type: "MATURE"; plot: number }
  | { type: "HARVEST"; plot: number };
```

事件和动作的默认映射：

```text
PLANT   → planting
CARE    → watering
MATURE  → noticing
HARVEST → celebrate
```

动作播放完成后自动回到 `idle`。动画不应阻塞用户点击其他地块，也不应因为缺少动画素材而阻止玩法继续。

## 8. 未来农场区域接口

未来新增土地时，使用区域清单，不把区域信息写死在场景组件中：

```ts
export type FarmZone = {
  id: string;
  name: string;
  unlocked: boolean;
  background: AssetRef;
  plotCount: number;
  assetFamily: string;
};
```

示例：

```ts
const zones: FarmZone[] = [
  {
    id: "main-field",
    name: "主农场",
    unlocked: true,
    background: farmAssets.scenes.main,
    plotCount: 10,
    assetFamily: "default",
  },
  {
    id: "east-field",
    name: "东侧新田",
    unlocked: false,
    background: farmAssets.scenes.lockedField!,
    plotCount: 6,
    assetFamily: "default",
  },
];
```

`FarmState` 中应保存区域 ID 和地块状态，不保存图片文件名。替换素材不会影响已有用户的存档。

## 9. PNG 与 GIF 兼容要求

- PNG 用于背景、地块、作物、装饰和静态回退图；
- GIF 用于刘看山和一次性反馈动画；
- PNG 和 GIF 都通过统一的图片组件加载；
- GIF 不建议作为 CSS `background-image`，应作为独立图片图层；
- 一次性 GIF 必须在清单中标记 `loop: false` 和 `durationMs`；
- 开启减少动态效果时，优先显示 `fallback`；
- 页面只挂载当前需要的动画，避免多个 GIF 同时解码；
- 非首屏素材可以延迟加载。

## 10. 素材交付清单

团队交付每个素材时，附带以下信息：

```text
素材名称：
文件路径：
用途：
尺寸：
是否透明：
是否循环：
动画时长：
默认回退图：
备注：
```

第一版最小素材包建议为：

```text
farm-main-day.png
plot-empty.png
plot-locked.png
seed.png
sprout.png
mature.png
mascot-idle.gif
mascot-planting.gif
mascot-watering.gif
mascot-celebrate.gif
effect-water.png
effect-harvest.gif
```

这套素材足以支撑全屏农场、三阶段成长、刘看山互动和收获反馈。后续再增加季节背景、作物主题、土地区域和装饰，不需要修改核心玩法。
