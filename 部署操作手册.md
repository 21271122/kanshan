# 《看山沃野》CloudBase 部署操作手册

> 目标：先把当前 Demo 部署为可公开访问的 HTTPS 链接。当前版本的农场进度使用浏览器演示存档；真实知乎 OAuth、收藏夹和 CloudBase 跨设备同步需要后续服务端接入后再发布。

## 0. 你会做什么

```text
创建 CloudBase 环境
→ 生成干净的上传包
→ 创建 HTTP 云函数
→ 配置 HTTP 网关路由
→ 获取 HTTPS 地址
→ 测试首页和 /api/health
```

不需要购买或维护云主机。

## 1. 开通 CloudBase 环境

1. 打开 [CloudBase 控制台](https://tcb.cloud.tencent.com/) 并登录你的腾讯云账号。
2. 选择“创建环境”或“开通云开发”。若首次使用，按腾讯云提示完成实名认证。
3. 环境名称填 `kanshan-woye-prod`；地域选择离你和评委较近的区域。
4. 在计费页先阅读价格与免费额度说明，再选择你能接受的方案；不要跳过费用确认。
5. 创建完成后，记下控制台显示的“环境 ID”。

此时不要创建 API Key，也不要把任何 Secret 粘进页面。

## 2. 生成上传包

在项目目录打开 PowerShell：

```powershell
cd D:\Projects\看山沃野
npm run package:cloudbase
```

命令会先运行生产构建，再生成一个类似下面的新文件夹：

```text
D:\Projects\看山沃野\.cloudbase-upload-20260913-153000
```

只上传这个新文件夹，不上传整个项目目录；这样不会上传 `node_modules`、`.env.local` 或安装 zhihu skill 时留下的临时文件。

> `scf_bootstrap` 必须无扩展名且使用 LF 换行。文件已经按此格式生成。若控制台报 `scf_bootstrap` 不存在或不能执行，请停止并把报错截图发给我；Windows 对 Unix 可执行位的处理需要针对报错调整。

## 3. 创建 HTTP 云函数

1. 在左侧进入“云函数”。
2. 点击“新建云函数”。
3. 选择 **HTTP 云函数**，不要选“普通云函数”。
4. 函数名称填 `kanshan-woye-web`。
5. 提交方式选择“本地上传文件夹”。
6. 选择第 2 步生成的 `.cloudbase-upload-...` 文件夹。
7. 运行环境选 **Node.js 20.19**（没有时选 Node.js 20）。
8. 开启“自动安装依赖”。内存先选 **512 MB**。
9. 点击创建并等待日志显示部署成功。

当前控制台可能显示“通过代码包创建”，而不是“本地上传文件夹”。两种方式都可以：选择第 2 步生成的目录，或上传该目录压缩后的 ZIP。上传包根目录必须直接包含 `scf_bootstrap`、`.next`、`public` 和 `package.json`，不能多套一层项目目录。

HTTP 云函数页面通常没有单独的“启动文件”输入框。CloudBase 会自动识别根目录中的无扩展名文件 `scf_bootstrap`。不要改名为 `scf_bootstrap.sh`。

CloudBase 的 Next.js 官方部署文档要求 `output: 'standalone'`、HTTP 云函数与 Node 20；这些项目配置已完成。[官方 Next.js 部署文档](https://docs.cloudbase.net/cloud-function/frameworks-examples/next)

## 4. 配置 HTTP 网关并测试地址

当前 CloudBase 控制台通过“HTTP 网关”发布 HTTP 云函数，不需要单独创建传统意义上的触发器。

1. 左侧进入 **HTTP 网关 → 域名及路由**。
2. 确认 HTTP 网关开关已开启，并找到系统提供的默认域名。
3. 点击“添加该域名路由”或“添加路由”，填写：

   ```text
   路由启用：开启
   访问路径：/
   关联资源类型：云函数
   关联资源：kanshan-woye-web
   跨域设置：开启
   路径透传：关闭即可
   身份认证：关闭（公开访问）
   ```

   若页面提供“全路径/通配路由”，优先选择全路径，以确保 `/api/health` 和静态资源也能访问。
4. 保存路由并等待发布完成。路由列表不再显示“暂无数据”后，再进行访问测试。
5. 在浏览器打开根路径，例如 `https://<默认域名>/`，应显示《看山沃野》。
6. 打开 `https://<默认域名>/api/health`，应返回：

```json
{"ok":true,"mode":"demo"}
```

如果访问默认域名出现 `INVALID_PATH`，通常表示 HTTP 网关域名存在但没有配置路由；回到第 4 步检查路由是否已保存并启用。

在首页测试：进入演示农场、收获引导果实、在空地播种、点击“演示加速”。

## 5. 后续开发与更新发布

修改本地代码后，不需要重新创建 CloudBase 环境、云函数或 HTTP 网关。只需更新同一个云函数：

1. 本地开发和测试：

   ```powershell
   Set-Location 'D:\Projects\看山沃野'
   npm run dev
   ```

2. 确认功能正常后生成生产上传包：

   ```powershell
   npm run package:cloudbase
   ```

   该命令会先执行 `npm run build`，然后生成新的 `.cloudbase-upload-时间戳` 目录。
3. 在 CloudBase 进入 **云函数/托管 → 函数管理 → kanshan-woye-web → 函数代码**。
4. 选择“上传代码”，上传最新的 `.cloudbase-upload-...` 文件夹或 ZIP。
5. 等待上传完成后点击“部署”。部署成功后，原来的 HTTP 网关地址保持不变。
6. 刷新线上首页，并重新测试 `/api/health` 和核心演示流程。

更新时注意：

- 不要上传整个项目目录；只上传脚本生成的最新目录。
- 不要把 `.env.local`、CloudBase Secret 或知乎密钥放入上传包。
- 不要修改或删除 `scf_bootstrap`。
- 如果更新失败，先查看云函数“日志”；需要时可在“版本及流量”中回退到上一个版本。
- 如果出现启动超时，可在函数配置中将内存从 256 MB 调高到 512 MB，并适当增加执行超时时间。

## 6. 提交前检查

- 用无痕窗口打开线上地址，确认没有依赖你的本地电脑。
- 在手机网络下再打开一次。
- 不提交 `CLOUDBASE_SECRET_KEY`、知乎 App Key、Access Secret 或 `.env.local`。
- 黑客松表单中的“可运行体验链接”填写根路径 HTTPS 地址，而不是 `/api/health`。

## 7. 下一阶段：真实账号数据

当前线上 Demo 验收通过后，再做：

1. 创建 `users`、`farms`、`crops`、`reviewed_items`、`achievements` 五个私有集合。
2. 在云函数环境变量中配置 CloudBase 与知乎 OAuth 密钥。
3. 完成 OAuth 回调、收藏夹读取和服务端数据库读写。
4. 拿到函数 HTTPS 域名后，将 `https://<域名>/api/auth/zhihu/callback` 精确填写到知乎应用的回调地址。

不要在当前仅演示模式下提前创建或填写这些 Secret。
